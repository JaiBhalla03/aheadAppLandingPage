import React from 'react';

const Hero = () => {
    return (
        <div className={'px-10 pt-20'}>
            <div className={'h-screen bg-[#eeebfe] rounded-[2rem]'}>
                this is the hero page that is going to be a little difficult so saving it for the last 
            </div>
        </div>
    );
};

export default Hero;